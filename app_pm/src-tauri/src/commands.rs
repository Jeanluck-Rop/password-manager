use core_pm::password_manager::PasswordManager;

#[tauri::command]
pub fn init_manager() {
}

/*
#[tauri::command(rename_all = "snake_case")]
pub fn open_db_request(path: &str,
		       key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn create_db_request(path: &str,
			 key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn new_password(path: &str,
		    key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn edit_password(path: &str,
		     key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn show_all_passwords(path: &str,
			  key: &) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn search_passwords(path: &str,
			key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn reveal_hidden_password(path: &str,
			      key: &str) {
}

#[tauri::command(rename_all = "snake_case")]
pub fn copy_to_clipboard_password(path: &str,
				  key: &str) {
}
*/
